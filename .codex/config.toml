# MCP configuration lives in .vscode/settings.json.

[mcp_servers.book-knowledge]
command = "uv"
args = ["run", "--directory", "../knowledge", "knowledge-mcp"]
env = { UV_CACHE_DIR = "/tmp/uv-cache" }
